name: best_architecture_challenge
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  http: ^0.13.3
  flutter_bloc: ^8.1.1
  equatable: ^2.0.5


dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^9.1.0
  json_serializable: ^6.3.1
  build_runner: ^2.2.0

flutter:

  uses-material-design: true

